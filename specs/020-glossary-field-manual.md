# 020 — Glossary + Field Manual (the teaching layer)
- [ ] Global <Term> + drawer; data in src/content/glossary.ts
- [ ] 60 entries (30 exist in builder-v0-3; add: consistency, consensus, failover,
      TTL, WAL, LSM-tree, B-tree, GSI, CDC, fan-out, backpressure, rate limiting,
      DNS, TLS handshake, connection pool, thundering herd, circuit breaker,
      bulkhead, blue-green, canary, CAP, quorum, leader election, exactly-once,
      at-least-once, visibility timeout, dead-letter queue, hot partition,
      read-your-writes, replication lag)
- [ ] Field Manual: 10 sections (5 exist; add: how the internet delivers a request,
      storage engines [B-tree vs LSM], replication & lag, partitioning & keys,
      delivery guarantees)
- [ ] Audit: every jargon word in player-facing copy is a <Term>; schema test enforces
